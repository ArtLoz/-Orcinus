<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
    Edge to edge: system bars are transparent and the app draws under them.
    The window background is OrcaSlicer's canvas colour, so nothing white shows
    while the first frame draws or behind the navigation bar.
    -->
    <style name="Theme.Orcinus" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/orca_canvas</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
